# IDK

This repository is a Python project scaffold with a virtual environment and a starter package directory.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
```

## Structure

- `base_gameplay/`: starter module directory
